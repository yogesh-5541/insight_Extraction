[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "langextract"
version = "0.1.0"
description = "Structured insight extraction library"
readme = "README.md"
requires-python = ">=3.9"

dependencies = []

[project.optional-dependencies]
openai = ["openai"]

[tool.setuptools]
packages = ["langextract"]
